import React from 'react';
import {Text, TextInput, View} from 'react-native';

const HomeScreen = () => {
    return (
        <View>
            <TextInput></TextInput>
        </View>
    );
};

export default HomeScreen;
